# Development overrides.
#
# Set your own APP_SECRET before running the dev environment. Production does
# not use this file — install.sh generates a unique secret into .env.local on
# each server, so no two installs share one.
APP_SECRET=set-your-own-dev-secret
